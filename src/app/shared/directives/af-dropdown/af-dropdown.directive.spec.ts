import { AFDropdownDirective } from './af-dropdown.directive';

describe('AfDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AFDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
